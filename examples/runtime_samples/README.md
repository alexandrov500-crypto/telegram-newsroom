# Runtime samples (v1.0.0 stable)

Sanitized JSON for **documentation and demos only**.

- **Do not** run `make verify-runtime` against this directory — `sha256` values are placeholder literals, not file hashes.
- **Do not** expect a passing `runtime-index` if only a subset of files is copied (14 artifacts required for OK).
- For a real inspection path, run `make runtime-nightly` then use `OUTPUT_DIR=./runtime_ops_output`.

See [docs/REAL_WORLD_VALIDATION.md](../../docs/REAL_WORLD_VALIDATION.md).
